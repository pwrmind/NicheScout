# WB NicheScout

Браузерное расширение для семантического анализа товаров на Wildberries. Расширение использует искусственный интеллект для оценки соответствия товаров поисковому запросу и отображает процент релевантности прямо на карточках товаров.

## 📋 Основные функции

- **Семантический анализ** — оценивает смысловое соответствие товаров поисковому запросу
- **Визуальные индикаторы** — отображает процент релевантности на каждой карточке товара
- **Автоматическая работа** — автоматически сканирует страницы Wildberries при навигации
- **Адаптивная производительность** — использует WebGPU при наличии, иначе переключается на WASM
- **Многоязычная модель** — работает с текстами на разных языках

## 🛠 Технологии

- **Transformers.js** — библиотека для запуска моделей машинного обучения в браузере
- **ONNX Runtime** — оптимизированная инференс-среда
- **WebGPU/WASM** — аппаратное ускорение или CPU-режим
- **Chrome Extensions API v3** — современный API расширений

## 📁 Структура проекта

```
├── manifest.json          # Конфигурация расширения
├── background.js          # Фоновый скрипт (оркестратор)
├── content.js            # Скрипт для работы с DOM Wildberries
├── offscreen.html        # Offscreen-документ для AI модели
├── offscreen.js          # Логика загрузки и работы модели
└── lib/                  # Библиотеки (transformers.min.js)
```

## ⚙️ Установка для разработки

1. Клонируйте репозиторий
2. Откройте Chrome и перейдите в `chrome://extensions/`
3. Включите "Режим разработчика" (Developer mode)
4. Нажмите "Загрузить распакованное расширение" (Load unpacked)
5. Выберите папку с файлами расширения

## 🚀 Как использовать

1. Установите расширение
2. Перейдите на сайт Wildberries (wb.ru)
3. Выполните поиск любого товара
4. На карточках товаров появятся цветные бейджи с процентом соответствия:
   - 🟢 Зеленый (70-100%) — высокая релевантность
   - 🟠 Оранжевый (<70%) — средняя/низкая релевантность

## 🔧 Технические детали

### Модель AI
- **Модель**: `Xenova/multilingual-e5-small`
- **Задача**: Feature extraction (извлечение эмбеддингов)
- **Тип данных**: FP32 для WebGPU, Q8 для WASM
- **Нормализация**: Mean pooling + L2 нормализация

### Архитектура
1. **Content Script** извлекает текст товаров и поисковый запрос
2. **Background Script** управляет offscreen-документом
3. **Offscreen Document** загружает и выполняет модель AI
4. **Cosine Similarity** вычисляет схожесть эмбеддингов
5. **UI Update** отображает результаты на странице

### Оптимизации
- Кеширование модели в браузере
- Автоматический выбор WebGPU/WASM
- Асинхронная обработка
- Избегание повторной инициализации модели

## ⚠️ Возможные проблемы и решения

### Медленная загрузка модели
- Первая загрузка может занять время (~20-50 МБ)
- Последующие запуски используют кеш браузера

### Отсутствие WebGPU
- Расширение автоматически переключится на WASM
- WASM работает медленнее, но стабильнее

### Блокировка ресурсов
- Убедитесь, что CDN для WASM файлов не блокируется
- Проверьте консоль разработчика на наличие ошибок CORS

## 🔒 Разрешения

Расширение требует:
- `offscreen` — для создания фонового документа с AI моделью
- `storage` — для кеширования модели
- `<all_urls>` — для работы на сайте Wildberries

## 📄 Лицензия

Это проект с открытым исходным кодом. Перед использованием убедитесь в соответствии с лицензиями используемых библиотек (Transformers.js, ONNX Runtime).

## 🤝 Вклад в развитие

Если вы нашли ошибку или хотите предложить улучшение:
1. Создайте Issue с описанием проблемы
2. Предложите Pull Request с изменениями
3. Убедитесь, что код соответствует стилю проекта

---

**Примечание**: Расширение предназначено для образовательных целей и демонстрации возможностей AI в браузере. Производительность может варьироваться в зависимости от оборудования и браузера.